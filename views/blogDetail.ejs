<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= blog.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
</head>
<body class="bg-light">

  <!-- Blog Container -->
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-8">

        <!-- Blog Card -->
        <div class="card shadow-lg border-0 rounded-4 p-4">
          <div class="card-body">
            
            <!-- Blog Title -->
            <h1 class="card-title fw-bold mb-3"><%= blog.title %></h1>
            
            <!-- Blog Meta -->
            <p class="text-muted mb-4">
              <small>By <strong><%= blog.author %></strong> • 
              <%= blog.createdAt.toDateString() %></small>
            </p>
            
            <!-- Blog Content -->
            <div class="fs-5 lh-lg text-dark" style="white-space: pre-line;">
              <%= blog.content %>
            </div>
            
            <!-- Back Button -->
            <div class="mt-4">
              <a href="/buy-copy" class="btn btn-outline-primary rounded-pill px-4">
                ← buy a copy of the book
              </a>
            </div>
          </div>
        </div>

      </div>
    </div>
  </div>

</body>
</html>

